url: "https://www.bayis.co.uk"
baseurl: ""

plugins:
  - jekyll-sitemap
  - jekyll-redirect-from

defaults:
  - scope:
      path: "library"
      type: "pages"
    values:
      layout: article

permalink: /library/:title.html
